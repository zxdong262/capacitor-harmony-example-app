# node
node_modules/
_lib/

# Vite build output (generated by `npm run build` into www/, then copied by
# `cap sync harmony` into harmony/rawfile/). Source lives in src/.
www/

# capacitor generated native project
harmony/

# harmony build outputs
oh_modules/
entry/build/
build/
.hvigor/
.cache/
.cxx/

# signing materials (NEVER commit)
signing/
*.p12
*.cer
*.p7b
*.csr

# downloaded native libs / headers
entry/libs/
entry/node-headers/

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db
